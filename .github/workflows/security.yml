name: Security

on:
  pull_request:
    branches: [main]
  release:
    types: [published]

permissions:
  contents: read
  pull-requests: write

jobs:
  security:
    uses: PRavaga/ci-security/.github/workflows/security.yml@v1
    secrets: inherit
