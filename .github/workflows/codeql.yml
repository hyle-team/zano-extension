name: CodeQL
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: "24 7 * * 1"
permissions:
  contents: read
  security-events: write
  actions: read
jobs:
  codeql:
    uses: PRavaga/ci-security/.github/workflows/codeql.yml@v1.2.0
